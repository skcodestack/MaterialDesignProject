<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="github.skcodestack.materialdesignproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlbumActivity" />
        <activity android:name=".RecyclerViewAlbumActivity" />
        <activity android:name=".test.TestTouchEventActivity" />
        <activity android:name=".test.SingalViewGroupActivity" />
        <activity android:name=".test.DoubleViewGroupActivity"></activity>
    </application>

</manifest>