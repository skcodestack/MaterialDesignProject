<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="github.skcodestack.materialdesignproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".paint.PaintActivity" />
        <activity android:name=".canvas.CanvasActivity" />
        <activity android:name=".path.PathActivity" />
        <activity android:name=".paint.BaseUserActivity" />
        <activity android:name=".paint.TextActivity" />
        <activity android:name=".paint.CircularProgressBarActivity" />
        <activity android:name=".paint.SlidingPriceBarActivity" />
        <activity android:name=".paint.ShaderActivity" />
        <activity android:name=".paint.filter.FilterActivity" />
        <activity android:name=".paint.filter.ColorFilterActivity" />
        <activity android:name=".path.WaveActivity" />
        <activity android:name=".path.PathMeasureActivity"></activity>
    </application>

</manifest>